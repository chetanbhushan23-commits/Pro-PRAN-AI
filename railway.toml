[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Intentionally omit startCommand so Railway uses Dockerfile CMD.
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
